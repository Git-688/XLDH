{
  "name": "xjdh-navigation",
  "version": "1.0.0",
  "description": "星聚导航 - 自动化部署",
  "scripts": {
    "lint": "eslint . --ext .js --ignore-path .eslintignore",
    "build": "node scripts/build.js",
    "deploy": "wrangler pages deploy ./dist --project-name=xjdh-navigation --branch=main"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "html-minifier": "^4.0.0",
    "clean-css": "^5.0.0",
    "terser": "^5.0.0"
  }
}
